{
  "id": 6,
  "title": "第 6 套 — 栈与队列",
  "focus": "围绕栈与队列的实现、典型应用（括号匹配、逆波兰表达式 RPN、FIFO 模拟）以及各种操作复杂度的训练。",
  "questions": [
    {
      "chapter": 3,
      "topic": "栈的 LIFO",
      "difficulty": 1,
      "type": "mcq",
      "statement_md": "栈（stack）的**基本原则**是什么？\n\n> 提示：想一想最后放进去的元素会在什么时候被取出。",
      "choices": [
        "最先放入的元素总是最先被取出（FIFO）",
        "最后放入的元素总是最先被取出（LIFO）",
        "元素会自动按升序排序",
        "任何时刻都只能访问最小元素"
      ],
      "answer": "B",
      "explanation_md": "**栈（Pile）**遵循 **LIFO** 原则（Last In, First Out，也就是后进先出）。\n\n最后压入栈的元素会最先弹出。可以把它想成一摞盘子：你总是从最上面先拿。\n\n**与 FIFO（队列 / File）对比**：队列遵循的是先进先出，就像排队买票。\n\n```python\n# 栈的操作\nP = []\nP.append(1)    # 压入 1：[1]\nP.append(2)    # 压入 2：[1, 2]\nx = P.pop()    # 弹出，得到 2（最后压入的）\n# P 现在是 [1]\n```"
    },
    {
      "chapter": 3,
      "topic": "队列的 FIFO",
      "difficulty": 1,
      "type": "mcq",
      "statement_md": "队列（queue / file）遵循什么原则？\n\n> 提示：想象一下排队的场景。",
      "choices": [
        "最后到的人先被服务（LIFO）",
        "最先到的人先被服务（FIFO）",
        "服务顺序是随机的",
        "任何时刻只能访问中间位置的元素"
      ],
      "answer": "B",
      "explanation_md": "**队列（File）**遵循 **FIFO** 原则（First In, First Out，也就是先进先出）。\n\n第一个进入队列的元素，会最先被移除。这就像窗口前排队：先到的人先办理。\n\n**常见操作**：\n- `ajouter_element(x, F)`：在队尾加入元素\n- `extraire_element(F)`：从队首取出并返回元素\n- `premier_element(F)`：查看队首元素（但不移除）\n- `est_vide(F)`：判断队列是否为空\n\n**典型应用**：广度优先搜索（BFS）、打印任务队列、消息队列、银行排队系统等。"
    },
    {
      "chapter": 3,
      "topic": "pop(0) 与 pop() 的复杂度",
      "difficulty": 2,
      "type": "mcq",
      "statement_md": "如果用 Python 的列表来实现一个**队列**：右侧用 `append` 加入元素，左侧用 `pop(0)` 取出元素。\n\n那么列表上的 `pop(0)` 的**复杂度**是多少？\n\n> 提示：思考 `pop(0)` 之后，剩余元素会发生什么。",
      "choices": [
        "O(1) —— 常数时间，非常高效",
        "O(log n) —— 对数时间",
        "O(n) —— 线性时间，因为其余元素都要左移",
        "O(n²) —— 二次时间"
      ],
      "answer": "C",
      "explanation_md": "**`pop(0)` 的复杂度是 O(n)**。\n\n因为当你删除列表开头的元素时，Python 必须把后面的所有元素都向左移动一位，来填补最前面的空位。\n\n**对比**：\n- `pop()`（从末尾弹出）：**O(1)**，不需要移动其他元素\n- `pop(0)`（从开头弹出）：**O(n)**，需要整体搬移\n\n例如：列表 `[1, 2, 3, 4, 5]`\n```\npop()   ->  直接移除 5，列表变成 [1,2,3,4]，复杂度 O(1)\npop(0)  ->  移除 1，2/3/4/5 都要左移一位，复杂度 O(n)\n```\n\n**经验结论**：如果真要高效实现队列，通常更适合使用 `collections.deque`。"
    },
    {
      "chapter": 1,
      "topic": "字符串与索引",
      "difficulty": 1,
      "type": "short",
      "statement_md": "下面 Python 表达式会输出什么？\n\n```python\ns = '(a+b)*c'\nprint(s[0], s[-1], s[3:6])\n```\n\n请写出**完全一致**的输出（元素之间保留空格）。\n\n> 提示：索引从 0 开始，负索引从末尾开始计数。",
      "answer": "( c b)*",
      "explanation_md": "逐个分析：\n\n```python\ns = '(a+b)*c'\n```\n\n字符与索引对应如下：\n```\n位置: 0  1  2  3  4  5  6\n字符: (  a  +  b  )  *  c\n```\n\n- `s[0]` = `'('`\n- `s[-1]` = `'c'`\n- `s[3:6]` = `'b)*'`（切片取索引 3、4、5，不含 6）\n\n因此：\n```python\nprint(s[0], s[-1], s[3:6])\n```\n会输出：\n```\n( c b)*\n```"
    },
    {
      "chapter": 2,
      "topic": "简单生成器",
      "difficulty": 2,
      "type": "short",
      "statement_md": "下面代码的**输出**是什么？\n\n```python\ndef gen(n):\n    for i in range(n):\n        yield i * 2\n\ng = gen(3)\nfor x in g:\n    print(x, end=' ')\n```\n\n请写出显示出来的内容。\n\n> 提示：yield 逐个产出值，循环会把它们依次消费。",
      "answer": "0 2 4",
      "explanation_md": "生成器 `gen(n)` 会依次产生 `0, 2, 4, ...` 这样的值。\n\n执行过程：\n1. `g = gen(3)` 创建生成器对象，此时**函数体还没有真正执行**\n2. `for x in g:` 开始迭代：\n   - 第一次：`i = 0`，yield `0`，输出 `0 `\n   - 第二次：`i = 1`，yield `2`，输出 `2 `\n   - 第三次：`i = 2`，yield `4`，输出 `4 `\n   - 之后生成器结束\n\n严格来说，打印时最后还会多一个空格；但题目通常按可见数值内容记作：`0 2 4`。"
    },
    {
      "chapter": 3,
      "topic": "手动跟踪栈",
      "difficulty": 2,
      "type": "short",
      "statement_md": "手动执行下面伪代码，并给出栈 `P` 的**最终状态**：\n\n```\nP ← pile_vide()\nempiler(3, P)\nempiler(5, P)\nempiler(1, P)\nx ← depiler(P)\nempiler(x + 2, P)\ndepiler(P)\n```\n\n请用 `[a, b, ...]` 的形式写出最终的 `P`。\n\n> 提示：逐步跟踪，并记住栈顶总是在列表末尾。",
      "answer": "[3, 5]",
      "explanation_md": "逐步追踪：\n\n1. `P ← pile_vide()` -> `P = []`\n2. `empiler(3, P)` -> `P = [3]`\n3. `empiler(5, P)` -> `P = [3, 5]`\n4. `empiler(1, P)` -> `P = [3, 5, 1]`\n5. `x ← depiler(P)` -> `x = 1`，`P = [3, 5]`\n6. `empiler(x + 2, P)` -> `empiler(3, P)`，所以 `P = [3, 5, 3]`\n7. `depiler(P)` -> 弹出栈顶 3，`P = [3, 5]`\n\n最终状态是：`[3, 5]`。"
    },
    {
      "chapter": 3,
      "topic": "手动跟踪队列",
      "difficulty": 2,
      "type": "short",
      "statement_md": "手动执行下面与**队列**有关的伪代码，并给出 `F` 的**最终状态**：\n\n```\nF ← file_vide()\najouter_element(2, F)\najouter_element(8, F)\nx ← extraire_element(F)\najouter_element(x + 1, F)\najouter_element(4, F)\ny ← extraire_element(F)\n```\n\n请把最终的 `F` 写成 `[a, b, ...]` 形式（左边是队首）。\n\n> 提示：队列是 FIFO，加入在末尾，取出在开头。",
      "answer": "[3, 4]",
      "explanation_md": "按 FIFO 规则逐步追踪：\n\n1. `F ← file_vide()` -> `F = []`\n2. `ajouter_element(2, F)` -> `F = [2]`\n3. `ajouter_element(8, F)` -> `F = [2, 8]`\n4. `x ← extraire_element(F)` -> 取出队首 2，所以 `x = 2`，`F = [8]`\n5. `ajouter_element(x + 1, F)` -> 加入 3，所以 `F = [8, 3]`\n6. `ajouter_element(4, F)` -> `F = [8, 3, 4]`\n7. `y ← extraire_element(F)` -> 再取出队首 8，所以 `F = [3, 4]`\n\n最终状态是：`[3, 4]`。"
    },
    {
      "chapter": 1,
      "topic": "整数与进制转换",
      "difficulty": 1,
      "type": "short",
      "statement_md": "下面这个 Python 表达式的结果是什么？\n\n```python\nresult = int('11', 2) + int('11', 16)\nprint(result)\n```\n\n> 提示：注意不同进制的转换。",
      "answer": "20",
      "explanation_md": "这里做了两个不同进制的转换：\n\n1. `int('11', 2)`：把二进制 `'11'` 转成十进制\n   - $1 \\times 2^1 + 1 \\times 2^0 = 2 + 1 = 3$\n\n2. `int('11', 16)`：把十六进制 `'11'` 转成十进制\n   - $1 \\times 16^1 + 1 \\times 16^0 = 16 + 1 = 17$\n\n3. 最后相加：`3 + 17 = 20`\n\n```python\n>>> int('11', 2)\n3\n>>> int('11', 16)\n17\n>>> 3 + 17\n20\n```"
    },
    {
      "chapter": 3,
      "topic": "栈的高度",
      "difficulty": 2,
      "type": "code",
      "statement_md": "实现函数 `hauteur_pile(P)`，返回栈 `P` 中的**元素个数**（且不修改 `P`）。\n\n示例：\n```\n>>> P = [1, 2, 3]\n>>> hauteur_pile(P)\n3\n>>> hauteur_pile([])\n0\n```\n\n函数签名：`hauteur_pile(P) -> int`",
      "answer": "返回 len(P)",
      "explanation_md": "最直接的实现就是：\n\n```python\ndef hauteur_pile(P):\n    return len(P)\n```\n\n如果题目禁止使用 `len()`，也可以递归统计：\n\n```python\ndef hauteur_pile(P):\n    if not P:\n        return 0\n    return 1 + hauteur_pile(P[1:])\n```\n\n但后者效率更低。\n\n**复杂度**：\n- 用 `len()`：O(1)\n- 递归版本：O(n) 时间，O(n) 额外栈空间"
    },
    {
      "chapter": 3,
      "topic": "括号配对",
      "difficulty": 3,
      "type": "code",
      "statement_md": "请写出算法 `bien_parenthesee(s)` 的**伪代码**：若字符串 `s` 中的括号**正确配对**（包括圆括号、方括号、大括号），则返回 `True`，否则返回 `False`。\n\n支持 `( )`、`[ ]`、`{ }`。\n\n伪代码格式（Moisan 风格）：\n- 赋值使用 `←`\n- 循环使用“对每个 ... 属于 ...”\n- 条件使用“如果 ... 否则 ...”\n- 缩进清晰",
      "answer": "使用栈追踪开括号，遇到闭括号时检查是否匹配",
      "explanation_md": "**伪代码**（按 Moisan 风格）：\n\n```\n函数 bien_parenthesee(s)  // s : 字符串 → bool\n  P ← 空栈()\n  对每个字符 c 属于 s\n    如果 c ∈ {'(', '[', '{'}\n      入栈(c, P)\n    否则如果 c ∈ {')', ']', '}'}\n      如果 栈为空(P)\n        返回 False\n      opening ← 出栈(P)\n      如果 (c == ')' 且 opening ≠ '(') 或\n         (c == ']' 且 opening ≠ '[') 或\n         (c == '}' 且 opening ≠ '{')\n        返回 False\n  返回 栈为空(P)\n```\n\n**对应的 Python 实现**：\n\n```python\ndef bien_parenthesee(s):\n    P = []\n    pairs = {'(': ')', '[': ']', '{': '}'}\n\n    for c in s:\n        if c in pairs:  # 开括号\n            P.append(c)\n        elif c in pairs.values():  # 闭括号\n            if not P or pairs[P.pop()] != c:\n                return False\n\n    return len(P) == 0\n```\n\n**复杂度**：O(n) 时间，O(n) 空间。"
    },
    {
      "chapter": 3,
      "topic": "括号匹配实现",
      "difficulty": 3,
      "type": "code",
      "statement_md": "请用 **Python** 实现函数 `est_bien_formee(expr)`：如果表达式 `expr`（字符串）中的括号 / 方括号 / 大括号都正确配对，则返回 `True`。\n\n测试样例：\n```\n>>> est_bien_formee('(a + [b * {c}])')\nTrue\n>>> est_bien_formee('([)]')\nFalse\n>>> est_bien_formee('(((')\nFalse\n>>> est_bien_formee('')\nTrue\n```",
      "answer": "使用栈匹配开闭括号，复杂度 O(n)",
      "explanation_md": "**完整实现**：\n\n```python\ndef est_bien_formee(expr):\n    pile = []\n    pairs = {'(': ')', '[': ']', '{': '}'}\n    \n    for char in expr:\n        if char in pairs:  # 开括号\n            pile.append(char)\n        elif char in pairs.values():  # 闭括号\n            if not pile or pairs[pile.pop()] != char:\n                return False\n    \n    return len(pile) == 0\n```\n\n**执行追踪** `est_bien_formee('([)]')`：\n1. `(` -> 压栈：`['(']`\n2. `[` -> 压栈：`['(', '[']`\n3. `)` -> 弹出得到 `[`，但它应该匹配 `]`，因此**不匹配**，直接返回 `False`\n\n**关键点**：\n- 遇到开括号就压栈\n- 遇到闭括号时：\n  - 若栈为空 -> 没有对应的开括号 -> `False`\n  - 若栈顶不匹配 -> `False`\n  - 若匹配 -> 弹栈并继续\n- 最终栈必须为空\n\n**复杂度**：O(n) 时间，O(n) 空间。"
    },
    {
      "chapter": 4,
      "topic": "简单类",
      "difficulty": 2,
      "type": "code",
      "statement_md": "实现一个类 `Point`，要求：\n- 构造函数 `__init__(self, x, y)` 初始化坐标\n- 方法 `distance_a_origine(self)` 返回到原点 `(0, 0)` 的欧几里得距离\n\n示例：\n```\n>>> p = Point(3, 4)\n>>> p.distance_a_origine()\n5.0\n>>> Point(0, 0).distance_a_origine()\n0.0\n```\n\n> 提示：可以使用 `math.sqrt()`。",
      "answer": "计算欧几里得距离 sqrt(x² + y²)",
      "explanation_md": "**实现**：\n\n```python\nimport math\n\nclass Point:\n    def __init__(self, x, y):\n        self.x = x\n        self.y = y\n    \n    def distance_a_origine(self):\n        return math.sqrt(self.x ** 2 + self.y ** 2)\n```\n\n**测试**：\n```python\n>>> p = Point(3, 4)\n>>> p.distance_a_origine()\n5.0\n\n>>> p2 = Point(1, 1)\n>>> p2.distance_a_origine()\n1.4142135623730951  # √2\n```\n\n公式是欧几里得距离：$d = \\sqrt{x^2 + y^2}$。\n\n**复杂度**：O(1)。"
    },
    {
      "chapter": 2,
      "topic": "递归生成器",
      "difficulty": 3,
      "type": "code",
      "statement_md": "实现一个**生成器** `sous_ensembles(L)`，用 `yield` 依次产生列表 `L` 的所有子集。\n\n示例：\n```\n>>> list(sous_ensembles([1, 2]))\n[[], [1], [2], [1, 2]]\n>>> list(sous_ensembles([]))\n[[]]\n```",
      "answer": "递归地生成所有子集组合",
      "explanation_md": "**递归思路**：\n- 空列表的子集只有一个：`[]`\n- 非空列表 `[x] + L` 的子集 = `L` 的子集，加上“每个子集再额外包含 x”的那一份\n\n**实现**：\n\n```python\ndef sous_ensembles(L):\n    if not L:\n        yield []\n    else:\n        for subset in sous_ensembles(L[1:]):\n            yield subset           # 不含 L[0]\n            yield [L[0]] + subset  # 含 L[0]\n```\n\n**执行追踪** `sous_ensembles([1, 2])`：\n1. `sous_ensembles([1, 2])` 调用 `sous_ensembles([2])`\n2. `sous_ensembles([2])` 调用 `sous_ensembles([])`\n3. `sous_ensembles([])` -> yield `[]`\n4. 回到 `sous_ensembles([2])`：\n   - yield `[]`\n   - yield `[2]`\n5. 回到 `sous_ensembles([1, 2])`：\n   - 从 `[]` 推出 `[]` 与 `[1]`\n   - 从 `[2]` 推出 `[2]` 与 `[1, 2]`\n\n最终得到：`[[], [1], [2], [1, 2]]`。\n\n**复杂度**：时间 O(2^n)，递归栈空间 O(n)。"
    },
    {
      "chapter": 3,
      "topic": "栈中的最大值",
      "difficulty": 3,
      "type": "code",
      "statement_md": "实现函数 `max_pile(P)`，返回栈 `P` 中的**最大元素**，并且**不修改** `P`。\n\n示例：\n```\n>>> max_pile([3, 1, 4, 1, 5])\n5\n>>> max_pile([10])\n10\n```\n\n复杂度是多少？",
      "answer": "遍历所有元素，复杂度 O(n)",
      "explanation_md": "**迭代实现**：\n\n```python\ndef max_pile(P):\n    if not P:\n        return None  # 或抛出异常\n    return max(P)\n```\n\n也可以手动遍历，不使用内置 `max()`：\n\n```python\ndef max_pile(P):\n    if not P:\n        return None\n    m = P[0]\n    for x in P[1:]:\n        if x > m:\n            m = x\n    return m\n```\n\n如果想用递归弹栈再恢复，也可以做到，但写法更复杂。\n\n**复杂度**：\n- 时间 O(n)\n- 额外空间 O(1)（若用迭代版本）"
    },
    {
      "chapter": 3,
      "topic": "栈元素求和",
      "difficulty": 2,
      "type": "code",
      "statement_md": "实现函数 `somme_pile(P)`，返回栈 `P` 中所有元素的**和**，且**不修改** `P`。\n\n示例：\n```\n>>> somme_pile([1, 2, 3, 4])\n10\n>>> somme_pile([])\n0\n```",
      "answer": "遍历元素求和，复杂度 O(n)",
      "explanation_md": "**最简单的实现**：\n\n```python\ndef somme_pile(P):\n    return sum(P)\n```\n\n也可以手写循环：\n\n```python\ndef somme_pile(P):\n    s = 0\n    for x in P:\n        s += x\n    return s\n```\n\n递归版本同样可行：\n\n```python\ndef somme_pile(P):\n    if not P:\n        return 0\n    return P[0] + somme_pile(P[1:])\n```\n\n**复杂度**：\n- 迭代：O(n) 时间，O(1) 空间\n- 递归：O(n) 时间，O(n) 栈空间"
    },
    {
      "chapter": 3,
      "topic": "栈的反转",
      "difficulty": 3,
      "type": "code",
      "statement_md": "实现函数 `inverser_pile(P)`，要求**原地修改**栈 `P`，把其中元素的顺序**反转**。\n\n示例：\n```\n>>> P = [1, 2, 3]\n>>> inverser_pile(P)\n>>> P\n[3, 2, 1]\n```\n\n注意：要直接修改 `P`，而不是新建一个栈作为结果。",
      "answer": "使用 reverse()，或构造反向列表后原地写回",
      "explanation_md": "**最简单的做法**：\n\n```python\ndef inverser_pile(P):\n    P.reverse()  # 原地反转\n```\n\n或者：\n\n```python\ndef inverser_pile(P):\n    P[:] = P[::-1]\n```\n\n如果题目要求显式模拟栈操作，也可以借助辅助栈完成，但代码会更长。\n\n**复杂度**：\n- `reverse()`：O(n) 时间，O(1) 额外空间\n- 用辅助结构：一般是 O(n) 时间，O(n) 空间"
    },
    {
      "chapter": 3,
      "topic": "逆波兰表达式求值",
      "difficulty": 4,
      "type": "code",
      "statement_md": "实现 `eval_rpn(s)`，用于**计算**一个后缀表达式（Reverse Polish Notation, RPN）。\n\n函数签名：`eval_rpn(s : str) -> float`\n\n字符串 `s` 由数字和运算符 `+`、`-`、`*`、`/` 组成，并且它们之间用空格分隔。\n\n示例：\n```\n>>> eval_rpn('3 4 +')\n7.0\n>>> eval_rpn('15 7 1 1 + - / 3 * 2 1 1 + + -')\n5.0\n>>> eval_rpn('2 3 4 * +')\n14.0\n```\n\n算法提示：用一个栈保存数字；每当遇到运算符时，就弹出两个数、计算，再把结果压回栈中。\n\n> 提示：可以用 `split()` 分词，数字转为 float，然后配合栈操作。",
      "answer": "用栈保存数字，遇到运算符就弹出计算后再压回",
      "explanation_md": "**算法思路**：\n1. 先把字符串按空格切成 token\n2. 逐个处理 token：\n   - 如果是数字，就压栈\n   - 如果是运算符，就弹出两个操作数，计算结果，再压回栈\n3. 最后栈里应只剩下一个值，就是答案\n\n**实现**：\n\n```python\ndef eval_rpn(s):\n    pile = []\n    tokens = s.split()\n    \n    for token in tokens:\n        if token in ['+', '-', '*', '/']:\n            b = pile.pop()  # 第二个操作数\n            a = pile.pop()  # 第一个操作数\n            \n            if token == '+':\n                result = a + b\n            elif token == '-':\n                result = a - b\n            elif token == '*':\n                result = a * b\n            else:\n                result = a / b\n            \n            pile.append(result)\n        else:\n            pile.append(float(token))\n    \n    return pile[0]\n```\n\n**例子** `eval_rpn('2 3 4 * +')`：\n1. 压入 2\n2. 压入 3\n3. 压入 4\n4. 遇到 `*`：弹出 4 和 3，得到 `3*4=12`，压回\n5. 遇到 `+`：弹出 12 和 2，得到 `2+12=14`，压回\n6. 返回 `14.0`\n\n**复杂度**：O(n) 时间，O(n) 空间。"
    },
    {
      "chapter": 3,
      "topic": "队列模拟",
      "difficulty": 4,
      "type": "code",
      "statement_md": "实现函数 `simulation_file(a, b, T)`，用来**模拟一个柜台前的排队系统**：\n\n- 客户每隔 `a` 秒到达一次（即在 `t=0, a, 2a, 3a, ...` 到达）\n- 每位客户的**服务时长**都是 `b` 秒\n- 模拟总时长为 `T` 秒\n- 返回一个二元组 `(nb_clients_servis, attente_max)`：\n  - `nb_clients_servis`：已经完成服务的客户数\n  - `attente_max`：观察到的最大等待时间（单位：秒）\n\n函数签名：`simulation_file(a : int, b : int, T : int) -> tuple`\n\n示例：\n```\n>>> simulation_file(3, 2, 10)\n(3, 2)\n```\n\n补充说明：\n- 到达发生在离散时刻 `0, a, 2a, ...`\n- 若客户在 `t < T` 时到达，即使开始服务或结束服务发生在 `T` 之后，也可以继续排队/服务\n- 某位客户的等待时间定义为：`服务开始时刻 - 到达时刻`\n\n> 提示：可以用队列模拟，并追踪每位客户的到达时间以及服务开始/结束时刻。",
      "answer": "做离散事件模拟，跟踪队列与服务台的忙闲状态",
      "explanation_md": "一种直接思路是做**事件驱动模拟**：\n\n1. 生成所有在 `t < T` 时刻到达的客户\n2. 用一个队列保存尚未开始服务客户的到达时间\n3. 用变量记录柜台下一次空闲的时刻\n4. 对每位客户：\n   - 其开始服务时刻 = `max(到达时刻, 柜台空闲时刻)`\n   - 等待时间 = `开始服务时刻 - 到达时刻`\n   - 服务结束时刻 = `开始服务时刻 + b`\n5. 若服务结束时刻 `<= T`，则该客户计入 `nb_clients_servis`\n6. 在整个过程中更新 `attente_max`\n\n一个可行实现如下：\n\n```python\ndef simulation_file(a, b, T):\n    arrivals = [t for t in range(0, T, a)]\n    next_free = 0\n    served = 0\n    attente_max = 0\n\n    for arrival in arrivals:\n        start = max(arrival, next_free)\n        wait = start - arrival\n        end = start + b\n\n        attente_max = max(attente_max, wait)\n        if end <= T:\n            served += 1\n\n        next_free = end\n\n    return served, attente_max\n```\n\n这里的关键不是某一种唯一写法，而是你是否正确建模了：到达、排队、开始服务、结束服务、等待时间，以及“完成服务”的判定。"
    },
    {
      "chapter": 4,
      "topic": "面向对象的栈",
      "difficulty": 3,
      "type": "code",
      "statement_md": "用面向对象方式实现一个 LIFO 栈类 `Pile`，内部使用列表 `self._t`。需要提供的方法：`__init__()`、`est_vide()`、`empiler(x)`、`depiler()`（空栈时用 assert）、`sommet()`、`__len__()`、`__repr__()`，其中 `__repr__()` 的格式应为 `'(bas) [...] (haut)'`。请给出完整代码。",
      "answer": "self._t = []；用 append/pop；空栈前先 assert；__repr__ 基于 self._t 返回字符串",
      "explanation_md": "```python\nclass Pile:\n    def __init__(self):\n        self._t = []\n    def est_vide(self):\n        return len(self._t) == 0\n    def empiler(self, x):\n        self._t.append(x)\n    def depiler(self):\n        assert not self.est_vide(), 'pile vide'\n        return self._t.pop()\n    def sommet(self):\n        assert not self.est_vide(), 'pile vide'\n        return self._t[-1]\n    def __len__(self):\n        return len(self._t)\n    def __repr__(self):\n        return f'(bas) {self._t} (haut)'\n```\n\n这里的 `append` / `pop` 都发生在列表末尾，因此摊销复杂度通常是 O(1)。`__repr__` 让 `print(p)` 时更容易看清栈的方向。"
    },
    {
      "chapter": 3,
      "topic": "两栈实现队列",
      "difficulty": 4,
      "type": "code",
      "statement_md": "这是一个经典技巧：用**两个**栈 `P_in` 和 `P_out` 来模拟一个队列（FIFO）。请用伪代码描述：\n\n1. `ajouter_element(x)`（入队 `x`）\n2. `extraire_element()`（出队）\n\n然后给出每次操作的**摊销复杂度**。",
      "answer": "入队：压入 P_in，O(1)；出队：若 P_out 为空，就把 P_in 全部倒到 P_out，再从 P_out 弹出；摊销 O(1)",
      "explanation_md": "**伪代码**：\n```\nfonction ajouter_element(x)\n  empiler(P_in, x)\n\nfonction extraire_element()\n  si est_vide(P_out)\n    tant que non est_vide(P_in)\n      empiler(P_out, depiler(P_in))\n  retourner depiler(P_out)\n```\n\n**为什么可行**：\n- 新元素一律先进 `P_in`\n- 当需要出队且 `P_out` 为空时，把 `P_in` 全部倒过去\n- 倒过去之后，最早进入 `P_in` 的元素会来到 `P_out` 的栈顶，于是就能按 FIFO 顺序弹出\n\n**摊销复杂度**：O(1)。\n\n虽然某一次 `extraire_element()` 可能会触发整批搬运，看起来是 O(k)，但从长期看，每个元素最多只会：\n- 进 `P_in` 一次\n- 出 `P_in` 一次\n- 进 `P_out` 一次\n- 出 `P_out` 一次\n\n因此总操作数与元素总数成正比，摊销下来每次仍是 O(1)。"
    }
  ]
}
